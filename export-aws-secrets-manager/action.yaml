name: Exports AWS Secrets Manager's secrets as environment variables
description: Exports AWS Secrets Manager's secrets as environment variables
runs:
  using: composite
  steps:
    - run: |
        echo '::warning::This action was deprecated. Please use suzuki-shunsuke/tfaction action.'
      shell: bash
    - uses: suzuki-shunsuke/tfaction@4f4ef14bad59c9af900162440f3ea24986df0491
      id: main
      with:
        action: export-aws-secrets-manager
