# scaffold-tfmigrate

Creates a PR that adds tfmigrate configuration files and migration file scaffolds to the specified root module.
If an existing PR is specified, pushes a commit to the existing PR instead of creating a new one.
